# Copy to .env for local development
DATABASE_URL="postgresql://rss_user:rss_password@localhost:5432/rss_db"

# Origin the frontend app is served from — used for CORS
FRONTEND_ORIGIN="http://localhost:3000"
